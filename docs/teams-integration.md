# Microsoft Teams Integration

Teams 整合讓團隊成員能在 Teams 中接收 AWS 資源狀態通知，無需登入 AWS Console。

## 架構

```
┌─────────────────────────────────────────────────────────────┐
│                     Microsoft Teams                         │
│  Channel: airsync-dev                                       │
└────────────────────────────────────▲───────────────────────┘
                                     │ Notification
┌────────────────────────────────────┴───────────────────────┐
│                     AWS Infrastructure                      │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  EventBridge Rule: ECS/RDS State Changes            │   │
│  └──────────┬──────────────────────────────────────────┘   │
│             │                                               │
│  ┌──────────▼──────────────────────────────────────────┐   │
│  │  Lambda: teams-notifier                             │   │
│  │  - Format Adaptive Card                             │   │
│  │  - POST to Teams Workflow Webhook                   │   │
│  └──────────┬──────────────────────────────────────────┘   │
│             │                                               │
│  ┌──────────▼──────────────────────────────────────────┐   │
│  │  SSM Parameter Store                                │   │
│  │  - /lights-out/{stage}/config (includes webhook)    │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 設定流程

### Step 1: 建立 Teams Workflow Webhook

1. 在 Teams channel 點擊 `...` → `Workflows`
2. 搜尋 **"Post to a channel when a webhook request is received"**
3. 配置並儲存
4. 複製 HTTP POST URL（格式：`https://prod-XX.logic.azure.com/...`）

### Step 2: 更新配置檔案

在專案的 YAML 配置檔案中加入 Teams webhook：

```yaml
# config/{account}/{project}.yml
notifications:
  teams:
    enabled: true
    webhook_url: 'https://prod-XX.logic.azure.com/...'
    description: 'Lights Out notifications'
```

### Step 3: 上傳配置

```bash
pnpm config
# 選擇目標環境 → Upload
```

### Step 4: 部署 Lambda

```bash
pnpm deploy
# 選擇目標環境 → All
```

### Step 5: 驗證

手動觸發資源狀態變更，確認 Teams channel 收到通知。

```bash
# 停止 ECS service
aws ecs update-service \
  --cluster <cluster> \
  --service <service> \
  --desired-count 0

# 預期：Teams channel 收到 🔴 STOPPED 通知
```

---

## 配置說明

### SSM 配置結構

Teams 通知配置整合在主要的 SSM config 中：

```yaml
notifications:
  teams:
    enabled: true
    webhook_url: 'https://prod-XX.logic.azure.com/...'
    description: 'Project notifications'
```

### 資源 Tag 對應

Teams 通知會根據資源的 tag 找到對應的專案配置：

```ini
lights-out:group=airsync-dev    # 對應 SSM 配置中的專案
```

---

## 成本

Teams 整合的 AWS 成本幾乎可忽略：

| 服務               | 估算           |
| ------------------ | -------------- |
| Lambda invocations | ~$0.01/月      |
| SSM Parameter      | 免費           |
| EventBridge        | 免費           |
| **總計**           | **< $0.02/月** |

---

## 常見問題

### 沒有收到通知

1. 確認 Lambda 有被觸發（查看 CloudWatch Logs）
2. 確認資源有 `lights-out:group` tag
3. 確認 SSM 配置中有設定 `notifications.teams`

### Webhook URL 洩漏處理

1. 在 Teams Workflows 中重新產生 URL
2. 更新 YAML 配置中的 `webhook_url`
3. 重新上傳配置：`pnpm config` → Upload

---

## 下一步

### Phase 2: 雙向指令（規劃中）

讓使用者可在 Teams 執行 start/stop/status 指令：

```
@LightsOutBot start airsync-dev
```

需要：

- Azure Bot 註冊
- API Gateway endpoint
- teams-bot-handler Lambda

詳見 [TODO.md](../TODO.md)
