# Create Pull Request

根據當前分支的變更內容，自動生成 PR 標題和描述，並發布 Pull Request。

## 執行步驟

1. **檢查 Git 狀態**
   - 執行 `git status` 確認工作目錄狀態
   - 如果有未提交的變更，提醒用戶先 commit 或 stash
   - 獲取當前分支名稱

2. **確認遠端分支**
   - 檢查當前分支是否已推送到遠端
   - 如果尚未推送，執行 `git push -u origin <branch-name>`

3. **分析變更內容**
   - 執行 `git log main..HEAD --oneline` 查看所有 commits
   - 執行 `git diff main...HEAD --stat` 查看變更檔案統計
   - 執行 `git diff main...HEAD` 查看詳細變更內容（如果太長可以只看關鍵部分）

4. **生成 PR 內容**
   根據分析結果，生成：
   - **標題**: 簡潔描述主要變更（遵循 conventional commits 格式，如 `feat:`, `fix:`, `refactor:` 等）
   - **描述**: 使用以下模板

   ```markdown
   ## Summary

   - <主要變更點 1>
   - <主要變更點 2>
   - <主要變更點 3>

   ## Test plan

   - [ ] <測試項目 1>
   - [ ] <測試項目 2>

   🤖 Generated with [Claude Code](https://claude.com/claude-code)
   ```

5. **確認並創建 PR**
   - 向用戶展示生成的標題和描述
   - 詢問用戶是否需要修改
   - 使用 `gh pr create` 創建 PR

## 注意事項

- 如果當前分支是 `main` 或 `master`，提醒用戶應該在 feature branch 上創建 PR
- PR 標題應該簡潔有力，不超過 72 個字元
- 描述應該包含足夠的上下文，讓 reviewer 能快速理解變更目的
- 遵循專案的 commit convention: `<type>(<scope>): <description>`
  - type: `feat|fix|docs|refactor|test`
  - scope: `core|discovery|handlers|config|infra`
